# language setting
language: node_js

# image setting
build_image: node:latest

# version numbers, testing against two versions of node
node_js:
 - 5

before_install:
 - ./installMongo.sh
 - ./startMongo.sh

before_script:
 - npm install
